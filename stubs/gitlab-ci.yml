# Generated by sead/laravel-api-contract — php artisan api:contract:install --ci=gitlab
# Re-run that command to regenerate this file.

image: php:8.3

stages:
  - test

cache:
  paths:
    - vendor/

api-contract-tests:
  stage: test
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
  before_script:
    - apt-get update && apt-get install -y unzip zip libzip-dev git
    - docker-php-ext-install pdo pdo_sqlite zip
    - curl -sS https://getcomposer.org/installer | php -- --install-dir=/usr/local/bin --filename=composer
  script:
    - composer install --no-interaction --prefer-dist --optimize-autoloader
    - cp .env.example .env
    - php artisan key:generate
    - php artisan test "{{TEST_PATH}}" {{TEST_FLAGS}}
